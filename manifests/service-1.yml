apiVersion: v1
kind: Service
metadata:
    name: "aksdemo-93ec"
    labels:
        app: "aksdemo-93ec"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aksdemo-93ec"